# whats-deployed configuration.
# Copy to .env and edit. Real environment variables override anything set here.

HOST_LABEL=prod-01
MAIN_BRANCH=main

# Milestone 2: compare against origin. Leave false until that is built.
CHECK_BEHIND=false

# Milestone 4: the add-project panel.
ALLOWED_ROOTS=/srv,/opt/apps

# The tracked projects themselves live in projects.json, not here.
